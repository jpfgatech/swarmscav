/**
 * Dummy test to verify test runner setup
 */

import { describe, it, expect } from 'vitest';

describe('Test Runner Setup', () => {
    it('should pass a simple arithmetic test', () => {
        expect(1 + 1).toBe(2);
    });
});
